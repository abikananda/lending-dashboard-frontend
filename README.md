# Lending Dashboard UI

Requires Node 20+.

```bash
npm install
npm start
npm run build
```

During local development, proxy `/api` to `http://localhost:8080` or serve the frontend and backend behind the same reverse proxy.
