import { Component,inject } from '@angular/core'; import { AsyncPipe,CurrencyPipe,DatePipe } from '@angular/common'; import { ApiService } from '../core/api.service'; import { catchError,of } from 'rxjs';
@Component({standalone:true,imports:[AsyncPipe,CurrencyPipe,DatePipe],template:`<h1>Portfolio Overview</h1><p class="text-secondary">Your lending performance and risk position</p>@if(summary$|async;as s){<section class="metrics">@for(m of metrics(s);track m.label){<div class="card p-3"><span class="text-secondary">{{m.label}}</span><strong>{{m.money?(m.value|currency:'INR':'symbol':'1.2-2'):m.value}}</strong></div>}</section>}<div class="card mt-4 p-3"><h4>Active and recent loans</h4><div class="table-responsive"><table class="table"><thead><tr><th>Scheme</th><th>Borrower</th><th>Invested</th><th>Received</th><th>Outstanding</th><th>EMI</th><th>DPD</th><th>Status</th></tr></thead><tbody>@for(l of loans$|async;track l.schemeId){<tr [class.table-danger]="l.npa"><td>{{l.schemeId}}</td><td>{{l.borrowerName||'—'}}</td><td>{{l.investedAmount|currency:'INR'}}</td><td>{{l.amountReceived|currency:'INR'}}</td><td>{{l.outstandingPrincipal|currency:'INR'}}</td><td>{{l.principalEmi|currency:'INR'}}</td><td>{{l.dpd}}</td><td><span class="badge" [class.bg-success]="l.loanStatus==='ACTIVE'" [class.bg-secondary]="l.loanStatus!=='ACTIVE'">{{l.loanStatus}}</span></td></tr>}@empty{<tr><td colspan="8" class="text-center p-5">Upload a loan report to begin.</td></tr>}</tbody></table></div></div>`,styles:[`.metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1rem}.metrics strong{font-size:1.5rem;display:block;color:#0f172a}`]}) export class DashboardComponent{private api=inject(ApiService);summary$=this.api.summary().pipe(catchError(()=>of(undefined)));loans$=this.api.loans().pipe(catchError(()=>of([])));metrics(s:any){return[{label:'Total lent',value:s.totalAmountLent,money:true},{label:'Total received',value:s.totalAmountReceived,money:true},{label:'Outstanding',value:s.outstandingPrincipal,money:true},{label:'Active loans',value:s.activeLoans},{label:'Closed loans',value:s.closedLoans},{label:'NPA loans',value:s.npaLoans}];}}
