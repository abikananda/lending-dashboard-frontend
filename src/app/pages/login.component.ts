import { Component,inject } from '@angular/core'; import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms'; import { Router } from '@angular/router'; import { AuthService } from '../core/auth.service';
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<div class="login"><form [formGroup]="form" (ngSubmit)="submit()" class="card shadow p-4"><h2>Lending Dashboard</h2><p class="text-secondary">Sign in to your LenDenClub portfolio</p><label>Username</label><input class="form-control mb-3" formControlName="username"><label>Password</label><input type="password" class="form-control mb-3" formControlName="password"><div class="text-danger mb-2">{{error}}</div><button class="btn btn-primary" [disabled]="form.invalid||loading">{{loading?'Signing in…':'Sign in'}}</button></form></div>`,styles:[`.login{min-height:100vh;display:grid;place-items:center;background:linear-gradient(135deg,#172554,#2563eb)}form{width:min(420px,92vw)}`]}) export class LoginComponent{private fb=inject(FormBuilder);private auth=inject(AuthService);private router=inject(Router);loading=false;error='';form=this.fb.nonNullable.group({username:['',Validators.required],password:['',Validators.required]});submit(){if(this.form.invalid)return;this.loading=true;this.auth.login(this.form.value.username!,this.form.value.password!).subscribe({next:()=>this.router.navigate(['/dashboard']),error:e=>{this.error=e.error?.message??'Login failed';this.loading=false;}});}}
